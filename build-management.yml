changelog:
    file: CHANGELOG.md
    envKey: VERSION

build:
    selections:
        rabbitMq:
            directory: src
            additionalTag: latest
            composeFileWithDigests: docker-compose.digest.yml
            files:
                - docker-compose.yml

publish:
    selections:
        rabbitMq:
            directory: src
            additionalTag: latest
            composeFileWithDigests: docker-compose.digest.yml
            files:
                - docker-compose.yml

swarm:
    selections:
        rabbitMq:
            directory: src
            files:
                - swarm-management.yml